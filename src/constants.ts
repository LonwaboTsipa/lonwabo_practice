import * as path from "path";
export const CLIENT_CODE_KEY = 'client_code';
export const ISIN_KEY = 'isin';
export const rootDir = path.resolve(process.cwd(), "artifacts");
export const pendingDir = path.resolve(rootDir, "pending");
